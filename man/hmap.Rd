% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataViz_heatmap.R
\name{hmap}
\alias{hmap}
\title{Create a Customized Heatmap for Count Data}
\usage{
hmap(
  tmm,
  title,
  deg = NULL,
  heatmap_colors = c("#5a78b3", "#93b2d3", "#e7f4f2", "#faf0ab", "#db7d54", "#b9332c"),
  metadata = NULL,
  metadata_col = NULL,
  annotation_colors = NULL,
  show_row_names = FALSE,
  show_column_names = FALSE,
  row_names_fontsize = NULL,
  row_names_fontface = NULL,
  column_names_fontsize = NULL,
  column_names_fontface = NULL
)
}
\arguments{
\item{tmm}{Numeric matrix or data.frame (rows = genes, cols = samples).}

\item{title}{Character. Heatmap title.}

\item{deg}{Optional character vector of genes to include (must match \code{rownames(tmm)}).}

\item{heatmap_colors}{Color vector for the heatmap gradient.}

\item{metadata}{Optional data.frame with sample metadata (rows = samples).}

\item{metadata_col}{Optional character. Column in \code{metadata} to use for top annotation.}

\item{annotation_colors}{Optional named vector mapping annotation levels to colors.}

\item{show_row_names, show_column_names}{Logical. Show gene/sample names. Default FALSE.}

\item{row_names_fontsize}{Numeric or NULL. Font size (points) for row names. NULL = default.}

\item{row_names_fontface}{Character/integer or NULL. Font face for row names
(e.g., \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}). NULL = default.}

\item{column_names_fontsize}{Numeric or NULL. Font size (points) for column names. NULL = default.}

\item{column_names_fontface}{Character/integer or NULL. Font face for column names
(e.g., \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}). NULL = default.}
}
\value{
A \code{ComplexHeatmap::Heatmap} object (drawn and returned).
}
\description{
This function generates a heatmap from a normalized count matrix (e.g., TMM or TPM data),
with the option to restrict the visualization to a set of differentially expressed genes (DEGs).
It supports a top annotation built from sample metadata and lets you control whether row/column
names are shown, plus their font size and font face (via ComplexHeatmap's \code{*_names_gp}).
}
\details{
If \code{annotation_colors} is provided but does not cover all annotation levels,
missing levels are filled with \code{"grey80"} and a warning is emitted.
}
